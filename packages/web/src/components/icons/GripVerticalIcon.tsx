'use client';

import React from 'react';
import { GripVertical } from 'lucide-react';

const GripVerticalIcon: React.FC<{ size: number }> = ({ size }) => {
  return <GripVertical size={size} />;
};

export default GripVerticalIcon;
